class User {
    constructor(username, email, profilePicture) {
        this.username = username;
        this.email = email;
        this.profilePicture = profilePicture;
    }
}

export default User;
